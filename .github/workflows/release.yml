name: Release

permissions:
  contents: write

on:
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Read version
        id: version
        run: echo "version=$(node -p "require('./manifest.json').version")" >> $GITHUB_OUTPUT

      - name: Generate changelog
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          PREV_TAG=$(git tag --sort=-version:refname | grep -v "^${VERSION}$" | head -1)

          if [ -z "$PREV_TAG" ]; then
            HEADER="## Initial Release"
            LOG=$(git log --pretty=format:"- %s (%an)" --no-merges)
          else
            HEADER="## Changes since ${PREV_TAG}"
            LOG=$(git log --pretty=format:"- %s (%an)" --no-merges "${PREV_TAG}..HEAD")
          fi

          {
            echo "${HEADER}"
            echo ""
            if [ -z "$LOG" ]; then
              echo "- No changes"
            else
              echo "${LOG}"
            fi
          } > RELEASE_NOTES.md

      - name: Create ZIP
        run: zip -j "notepack-obsidian-${{ steps.version.outputs.version }}.zip" main.js manifest.json styles.css

      - name: Create or update release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION="${{ steps.version.outputs.version }}"

          gh release create "$VERSION" \
            --title "NotePack ${VERSION}" \
            --notes-file RELEASE_NOTES.md \
            --latest \
            "notepack-obsidian-${{ steps.version.outputs.version }}.zip" \
            "main.js" \
            "manifest.json" \
            "styles.css"
